# Base URL del backend Hono.
# In dev locale tieni localhost; in produzione punta al Synology / Tailscale.
VITE_API_BASE_URL=http://localhost:3000

# Bearer token API. Deve corrispondere ad API_TOKEN del backend.
# Genera con: openssl rand -base64 32
VITE_API_TOKEN=
